<!DOCTYPE html>

<html>

<head>
    <title>Habitify - Build new habits</title>
    <link rel="stylesheet" href="../assets/css/index.css">
    <link rel="stylesheet" href="../assets/fontawesome/css/all.css">
</head>

<body>
    <div id="main">
        <br><br><br>
        <!-- add habit button -->
        <div id="addhabit" onclick="showHabitAdd()">
            <i class="far fa-plus-square"></i> &nbsp;Add a new habit
        </div>

        <!-- new habit creatiion <div> -->
        <div class="new-habit-div">
            <input type="text" id="newHabitName" placeholder="Enter name..">
            <select name="freq" id="newHabitFreq">
                <option value="7">Daily</option>
                <option value="1">Once in a week</option>
                <option value="2">Twice in a week</option>
                <option value="3">Thrice in a week</option>
                <option value="4">4 times in a week</option>
                <option value="5">5 times in a week</option>
                <option value="6">6 times in a week</option>
            </select>
            <button id="submit_new_habit" onclick="addNewHabit()">Create habit</button>
        </div>

        <!-- ejs for loop to dynamically show all the habits! -->
        <% for(let i = 0;i<habits.length;i++){%>
        <div class="listitem" id="post-<%= habits[i]._id %>"
            onclick="window.location.href='/habit?id=<%=habits[i]._id%>'">
            <div class="listtitle"><%= habits[i].name %> <button class="delbtn"
                    onclick="deletehabit(event ,'<%= habits[i]._id %>')"><i class="fa fa-times" aria-hidden="true"></i>
                </button> </div>
            <div class="list-day-container">
                <%for(let x = 0;x<7;x++){%>
                <div class="listday">
                    <span class="daytitle"><%= getStaticDay(x) %></span> <br>
                    <span <% if(success(x , habits[i].succeson)){ %> style="background-color:#5666F3;color:white" <%}%>
                                <% if(fail(x , habits[i].failon)){ %>
                        style="background-color:red;color:white;border:1.5px solid red" <%}%>
                                class="daynumber"><%= getStaticDate(x) %></span>
                </div>
                <%}%>
                </div>

                <!-- stats of the habit -->
                <div class="stats">
                    <span class="best">Best: <%= habits[i].longeststreak %> </span>
                <span class="freq">Freq: <%= habits[i].freq %></span>
            </div>
        </div>
        <%}%>


    </div>
    <script src ="../assets/js/index.js"></script>
</body>

</html>